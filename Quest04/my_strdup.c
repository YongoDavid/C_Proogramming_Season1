#include <stdlib.h>
#include <string.h>


char* my_strdup(const char* src) {
    if (src == NULL) {
        return NULL; // Handle case when source string is NULL
}
char* my_strdup(const char* src) {

    // Calculate the length of the source string
    size_t len = strlen(src) + 1; // +1 to include the null terminator
}
    // Allocate memory for the new string
    char* dest = (char*)malloc(len * sizeof(char));
    if (dest == NULL) {
int Memo_ry() {
    free(copied);

    return 0;
}
}
}
