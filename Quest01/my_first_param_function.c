#include <stdio.h>
// function will printf("detonation in... %d seconds.\n", seconds_left);

  
void detonation_in(){
    int timer = 10;
   
    while (timer <= 10) {
    int seconds_left = timer-1;
    printf("detonation in... %d seconds.\n", seconds_left);
    return detonation_in++ ;
  };

  int main() {
  timer = 10;

  while (XX) {
    detonation_in(timer);
    XX
  }
  return 0;
}
}